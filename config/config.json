{
  "development": {
    "dialect": "sqlite",
    "storage": "dev.sqlite"
  },
  "test": {
    "dialect": "sqlite",
    "storage": "test.sqlite"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  }
}
